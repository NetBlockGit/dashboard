import React from "react";
import "./App.css";
import Uses_numbers from "./components/Uses_number"
// import Uses_numbers from "./components/Uses_number"

function App() {
  return (

    <Uses_numbers queryProcess='145'/>
    
  );
}

export default App;
